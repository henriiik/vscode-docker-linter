{
  "name": "docker-linter-server",
  "version": "0.1.3",
  "author": "Henrik Sjööh <hello@enhenrik.nu> (http://www.enhenrik.nu)",
  "license": "MIT",
  "engines": {
    "node": "*"
  },
  "dependencies": {
    "vscode-languageserver": "^2.1.2"
  },
  "devDependencies": {
    "tslint": "^3.7.4",
    "typescript": "^1.8.10"
  },
  "scripts": {
    "compile": "installServerIntoExtension ../docker-linter ./package.json ./tsconfig.json && tsc -p .",
    "watch": "installServerIntoExtension ../docker-linter ./package.json ./tsconfig.json && tsc --watch -p ."
  }
}
