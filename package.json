{
	"name": "vscode-docker-perl",
	"description": "",
	"version": "0.0.1",
	"publisher": "henriiik",
	"engines": {
		"vscode": "*"
	},
	"dependencies": {
		"vscode-languageworker": "*"
	},
	"devDependencies": {
		"typescript": "^1.6.2"
	},
	"contributes": {
		"configuration": {
			"type": "object",
			"title": "vscode-docker-perl configuration",
			"properties": {
				"vscode-docker-perl.enable": {
					"type": "boolean",
					"default": false,
					"description": "Control whether vscode-docker-perl is enabled for perl files or not."
				}
			}
		},		
		"languageWorkers": {
			"activate": ["onLanguage:perl"],
			"executable": {
				"run": {
					"nodeModule": "out/extension.js"
				},
				"debug": {
					"nodeModule": "out/extension.js",
					"execArgv": ["--nolazy", "--debug=5871"]
				}
			},
			"settings": "vscode-docker-perl"
		}
	},
	"scripts": {
		"vscode:prepublish": "./node_modules/.bin/tsc",
		"compile": "./node_modules/.bin/tsc -watch -p ./"
	}
}