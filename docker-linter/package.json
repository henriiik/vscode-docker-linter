{
  "name": "docker-linter",
  "description": "",
  "version": "0.0.4",
  "author": "Henrik Sjööh <hello@enhenrik.nu> (http://www.enhenrik.nu)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Microsoft/vscode-eslint.git"
  },
  "bugs": {
    "url": "https://github.com/Microsoft/vscode-eslint/issues"
  },
  "categories": [
    "Languages",
    "Linters"
  ],
  "publisher": "henriiik",
  "engines": {
    "vscode": "0.10.x"
  },
  "activationEvents": [
    "onLanguage:perl",
    "onLanguage:python"
  ],
  "main": "./out/extension",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "docker-linter configuration",
      "properties": {
        "docker-linter.enable": {
          "type": "boolean",
          "default": false,
          "description": "Control whether docker-linter is enabled for perl files or not."
        },
        "docker-linter.machine": {
          "type": "string",
          "default": "default",
          "description": "The name of the docker-machine in which you run your linting container."
        },
        "docker-linter.container": {
          "type": "string",
          "default": "docker-linter",
          "description": "The name of the docker container in which the linting command is to be run."
        },
        "docker-linter.command": {
          "type": "string",
          "default": "perl -c",
          "description": "The command to which the buffer contents is sent."
        },
        "docker-linter.regexp": {
          "type": "string",
          "default": "(.*) at ([^ ]*) line (\\d+)[.,]",
          "description": "The regex that is is used to match for errors."
        },
        "docker-linter.message": {
          "type": "number",
          "default": 1,
          "description": "The number of the group that matches the error."
        },
        "docker-linter.line": {
          "type": "number",
          "default": 3,
          "description": "The number of the group that matches the line."
        },
        "docker-linter.column": {
          "type": "number",
          "default": 0,
          "description": "The number of the group that matches the column."
        },
        "docker-linter.severity": {
          "type": "number",
          "default": 0,
          "description": "The number of the group that matches the severity."
        },
        "docker-linter.code": {
          "type": "number",
          "default": 0,
          "description": "The number of the group that matches the code."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "cd ../docker-linter-server && npm run compile && cd ../docker-linter && node ./node_modules/vscode/bin/compile",
    "compile": "node ./node_modules/vscode/bin/compile -watch -p ./"
  },
  "dependencies": {
    "vscode-languageclient": "0.10.x"
  },
  "devDependencies": {
    "vscode": "0.10.x",
    "tslint": "^2.5.1"
  }
}